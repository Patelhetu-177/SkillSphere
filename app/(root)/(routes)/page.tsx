export const dynamic = "force-dynamic";



interface RootPageProps {
  searchParams: {
    name: string;
  };
}

const RootPage = async ({}: RootPageProps) => {
 

  return (
    <div className="h-full p-4 space-y-2">
    </div>
  );
};

export default RootPage;
